import React, { Component, PropTypes } from 'react'
import { sizeMake } from '../Shortcuts'

export default class CollectionViewLayout extends Component {
  collectionView = null
  collectionViewContentSize = sizeMake(0, 0)

  prepare() {
  }

  targetContentOffset(offset, velocity) {
  }

  layoutAttributesForElements(rect) {
  }

  layoutAttributesForItem(indexPath) {
  }

  shouldInvalidateLayout(rect) {
  }

}
